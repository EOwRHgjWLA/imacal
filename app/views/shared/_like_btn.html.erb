<% if current_user.likes?(party) %>
  <%= link_to "いいねを取り消す", unlike_party_path(party), method: :delete, remote: true, class: "btn btn-danger btn-sm" %>
<% else %>
  <%= link_to "いいね", like_party_path(party), method: :post, remote: true, class: "btn btn-outline-danger btn-sm" %>
<% end %>